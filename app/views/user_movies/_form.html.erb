<%= form_for(@evaluated_movie) do |f| %>
  <% if @evaluated_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evaluated_movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @evaluated_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :star %><br>
    <%= f.number_field :star %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
